# Temporal Collapse External-Source Raw Trajectory Validator — v6 Result

## Status

PASS.

The Level 3 v6 external-source raw trajectory validator executed successfully.

The validator script executed successfully.

No runtime error was produced.

```text
STDERR: empty
RETURN CODE: 0
```

Input file:

```text
data/temporal_collapse_external_source_raw_trajectories_v6.jsonl
```

Output file:

```text
results/temporal_collapse_external_source_raw_trajectory_validator_v6.json
```

Validator script:

```text
examples/temporal_collapse_external_source_raw_trajectory_validator_v6.py
```

---

## Purpose

This experiment moves Level 3 from separate-generator validation to declared external-source validation.

Level 3 v5 applied the raw trajectory warning mechanism to records generated by a separate generator script.

Level 3 v6 applies the same raw trajectory warning mechanism to declared external-source raw ordered trajectory records.

The objective is to move from:

```text
separate-generator validation
```

to:

```text
declared external-source validation
```

```text
measurement != inference != decision
```

---

## Tested Boundary

This v6 result is bounded.

The tested boundary is:

```text
declared external-source raw ordered trajectory records
```

Source:

```text
benchmark_prompt_perturbation_v6
```

Source independence:

```text
external_source_declared
```

Independence method:

```text
prompt_perturbation_trace
```

This is stronger than v5 as a declared source boundary.

But it does not prove absolute external verification.

Correct reading:

```text
v6 validates the warning mechanism on declared external-source raw trajectory records.
```

Incorrect reading:

```text
v6 proves externally verified universal validation.
```

The source independence is:

```text
external_source_declared
```

not:

```text
external_source_verified
```

---

## Input Dataset

The dataset contains five declared external-source raw trajectories:

```text
external_source_stable_001
external_source_drift_001
external_source_borderline_critical_001
external_source_critical_001
external_source_collapse_like_001
```

Each event contains:

```text
trajectory_id
step
signature
cluster
delta
iri
boundary_distance
phase
source
source_independence
independence_method
```

The validator groups events by `trajectory_id` and sorts each trajectory by `step`.

---

## Source Summary

The v6 source summary was:

```json
[
  {
    "source": "benchmark_prompt_perturbation_v6",
    "source_independence": "external_source_declared",
    "independence_method": "prompt_perturbation_trace",
    "trajectory_count": 5,
    "average_risk_score": 0.451667,
    "regime_counts": {
      "CRITICAL": 2,
      "COLLAPSE": 1,
      "DRIFT": 1,
      "STABLE": 1
    },
    "highest_risk_trajectory": "external_source_collapse_like_001",
    "highest_risk_score": 0.813406
  }
]
```

Interpretation:

All trajectories came from one declared external-source boundary.

The independence method was explicitly recorded as `prompt_perturbation_trace`.

This improves the boundary compared to v5, but it still does not establish `external_source_verified`.

---

## Risk Formula

The v6 risk score preserves the raw trajectory formula used by v3, v4, and v5:

```text
risk_score =
    0.20 * transition_density
  + 0.20 * drift_progression
  + 0.25 * boundary_proximity
  + 0.25 * collapse_similarity
  + 0.10 * irreversibility_signal
```

Weights:

```json
{
  "transition_density": 0.2,
  "drift_progression": 0.2,
  "boundary_proximity": 0.25,
  "collapse_similarity": 0.25,
  "irreversibility_signal": 0.1
}
```

Thresholds:

```text
risk_score < 0.25         -> STABLE
0.25 <= risk_score < 0.50 -> DRIFT
0.50 <= risk_score < 0.75 -> CRITICAL
risk_score >= 0.75        -> COLLAPSE
```

Gate actions:

```text
STABLE   -> PASS
DRIFT    -> WATCH
CRITICAL -> ESCALATE
COLLAPSE -> STOP
```

All weights and thresholds are visible.

No hidden interpretation layer is used.

---

## Aggregate Result

The aggregate v6 result was:

```text
aggregate_risk_score:  0.451667
aggregate_risk_regime: DRIFT
aggregate_gate_action: WATCH
source_count:          1
```

Regime counts:

```text
STABLE   -> 1
DRIFT    -> 1
CRITICAL -> 2
COLLAPSE -> 1
```

Highest-risk trajectory:

```text
external_source_collapse_like_001 -> 0.813406
```

Interpretation:

The aggregate result is DRIFT because the dataset contains a mixed set of trajectories.

The main result is not the aggregate alone.

The main result is the coherent separation of risk regimes under the declared external-source boundary.

---

## Ordered Risk Progression

The validator produced the following progression:

```text
external_source_stable_001               -> STABLE   -> 0.040000 -> PASS
external_source_drift_001                -> DRIFT    -> 0.294956 -> WATCH
external_source_borderline_critical_001  -> CRITICAL -> 0.503233 -> ESCALATE
external_source_critical_001             -> CRITICAL -> 0.606739 -> ESCALATE
external_source_collapse_like_001        -> COLLAPSE -> 0.813406 -> STOP
```

The borderline trajectory is important:

```text
external_source_borderline_critical_001 -> 0.503233
```

It crosses the CRITICAL threshold:

```text
CRITICAL threshold: 0.50
```

This differs from v5, where the borderline trajectory remained slightly below the CRITICAL threshold.

In v6, the borderline case enters CRITICAL because the declared external-source signals are slightly stronger.

---

## Individual Results

### external_source_stable_001

```text
source:              benchmark_prompt_perturbation_v6
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
risk_regime:         STABLE
risk_score:          0.040000
gate_action:         PASS
dominant_axis:       boundary_proximity
warning_flags:       []
```

Signals:

```json
{
  "transition_density": 0.0,
  "drift_progression": 0.0285,
  "boundary_proximity": 0.095,
  "collapse_similarity": 0.0346,
  "irreversibility_signal": 0.019
}
```

Evidence:

```json
{
  "signature_changes": 0.0,
  "cluster_changes": 0.0,
  "phase_changes": 0.0,
  "delta_early_mean": 0.0425,
  "delta_late_mean": 0.055,
  "iri_early_mean": 0.011,
  "iri_late_mean": 0.018,
  "min_boundary_distance": 0.9,
  "max_boundary_proximity": 0.1,
  "collapse_phase_count": 0,
  "source": "benchmark_prompt_perturbation_v6",
  "source_independence": "external_source_declared",
  "independence_method": "prompt_perturbation_trace"
}
```

Interpretation:

The stable trajectory remained structurally consistent.

No warning flags were emitted.

---

### external_source_drift_001

```text
source:              benchmark_prompt_perturbation_v6
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
risk_regime:         DRIFT
risk_score:          0.294956
gate_action:         WATCH
dominant_axis:       boundary_proximity
warning_flags:       []
```

Signals:

```json
{
  "transition_density": 0.333333,
  "drift_progression": 0.247944,
  "boundary_proximity": 0.455,
  "collapse_similarity": 0.202,
  "irreversibility_signal": 0.1445
}
```

Evidence:

```json
{
  "signature_changes": 0.5,
  "cluster_changes": 0.25,
  "phase_changes": 0.25,
  "delta_early_mean": 0.1125,
  "delta_late_mean": 0.313333,
  "iri_early_mean": 0.0365,
  "iri_late_mean": 0.124,
  "min_boundary_distance": 0.505,
  "max_boundary_proximity": 0.495,
  "collapse_phase_count": 0,
  "source": "benchmark_prompt_perturbation_v6",
  "source_independence": "external_source_declared",
  "independence_method": "prompt_perturbation_trace"
}
```

Interpretation:

The drift trajectory crossed out of STABLE and entered DRIFT.

No hard warning flags were emitted.

---

### external_source_borderline_critical_001

```text
source:              benchmark_prompt_perturbation_v6
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
risk_regime:         CRITICAL
risk_score:          0.503233
gate_action:         ESCALATE
dominant_axis:       boundary_proximity
warning_flags:
  - high_transition_density
  - boundary_proximity
```

Signals:

```json
{
  "transition_density": 0.583333,
  "drift_progression": 0.408667,
  "boundary_proximity": 0.728333,
  "collapse_similarity": 0.349667,
  "irreversibility_signal": 0.353333
}
```

Evidence:

```json
{
  "signature_changes": 0.75,
  "cluster_changes": 0.5,
  "phase_changes": 0.5,
  "delta_early_mean": 0.19,
  "delta_late_mean": 0.525,
  "iri_early_mean": 0.0925,
  "iri_late_mean": 0.311667,
  "min_boundary_distance": 0.225,
  "max_boundary_proximity": 0.775,
  "collapse_phase_count": 0,
  "source": "benchmark_prompt_perturbation_v6",
  "source_independence": "external_source_declared",
  "independence_method": "prompt_perturbation_trace"
}
```

Interpretation:

The borderline trajectory entered CRITICAL by a narrow margin.

This is structurally useful because the result exposes the threshold boundary.

The trajectory crossed the CRITICAL threshold by:

```text
0.503233 - 0.500000 = 0.003233
```

The validator did not hide the borderline nature of the result.

---

### external_source_critical_001

```text
source:              benchmark_prompt_perturbation_v6
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
risk_regime:         CRITICAL
risk_score:          0.606739
gate_action:         ESCALATE
dominant_axis:       boundary_proximity
warning_flags:
  - high_transition_density
  - boundary_proximity
```

Signals:

```json
{
  "transition_density": 0.75,
  "drift_progression": 0.492444,
  "boundary_proximity": 0.823333,
  "collapse_similarity": 0.420667,
  "irreversibility_signal": 0.4725
}
```

Evidence:

```json
{
  "signature_changes": 1.0,
  "cluster_changes": 0.75,
  "phase_changes": 0.5,
  "delta_early_mean": 0.26,
  "delta_late_mean": 0.658333,
  "iri_early_mean": 0.13,
  "iri_late_mean": 0.42,
  "min_boundary_distance": 0.135,
  "max_boundary_proximity": 0.865,
  "collapse_phase_count": 0,
  "source": "benchmark_prompt_perturbation_v6",
  "source_independence": "external_source_declared",
  "independence_method": "prompt_perturbation_trace"
}
```

Interpretation:

The critical trajectory entered CRITICAL without requiring explicit COLLAPSE markers.

The validator emitted ESCALATE based on transition pressure and boundary proximity.

---

### external_source_collapse_like_001

```text
source:              benchmark_prompt_perturbation_v6
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
risk_regime:         COLLAPSE
risk_score:          0.813406
gate_action:         STOP
dominant_axis:       boundary_proximity
warning_flags:
  - high_transition_density
  - high_drift_progression
  - boundary_proximity
  - collapse_similarity
  - irreversibility_signal
```

Signals:

```json
{
  "transition_density": 0.75,
  "drift_progression": 0.645778,
  "boundary_proximity": 0.945,
  "collapse_similarity": 0.852,
  "irreversibility_signal": 0.85
}
```

Evidence:

```json
{
  "signature_changes": 1.0,
  "cluster_changes": 0.75,
  "phase_changes": 0.5,
  "delta_early_mean": 0.345,
  "delta_late_mean": 0.868333,
  "iri_early_mean": 0.21,
  "iri_late_mean": 0.76,
  "min_boundary_distance": 0.02,
  "max_boundary_proximity": 0.98,
  "collapse_phase_count": 2,
  "source": "benchmark_prompt_perturbation_v6",
  "source_independence": "external_source_declared",
  "independence_method": "prompt_perturbation_trace"
}
```

Interpretation:

The collapse-like trajectory entered COLLAPSE.

All warning flags were activated.

The highest-risk trajectory was correctly identified as:

```text
external_source_collapse_like_001
```

---

## Structural Reading

The v6 result extends v5 by improving the declared source boundary.

v5 used raw trajectories generated by a separate internal generator script:

```text
source_independence: generated_by_independent_script
independence_method: separate_generator_script
```

v6 uses declared external-source raw trajectories:

```text
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
```

This is a stronger source-boundary step.

But it is not absolute external verification.

The correct structural reading is:

```text
mechanism remains coherent over declared external-source raw ordered trajectory records
```

not:

```text
mechanism is universally validated
```

---

## Main Finding

Safe claim:

```text
OMNIA-VALIDATION Level 3 v6 applied the raw trajectory warning mechanism
to declared external-source raw ordered trajectory records.

The validator separated STABLE, DRIFT, CRITICAL, and COLLAPSE regimes,
with the borderline trajectory crossing the CRITICAL threshold by a narrow margin.
```

---

## Important Limitation

This v6 result is still bounded.

The trajectories are declared external-source records under the source label:

```text
benchmark_prompt_perturbation_v6
```

The independence method is declared as:

```text
prompt_perturbation_trace
```

But the source independence is not:

```text
external_source_verified
```

Correct interpretation:

```text
Level 3 v6 validates the raw trajectory warning mechanism
on declared external-source raw ordered trajectory records.
```

Not:

```text
Level 3 v6 proves absolute external-source verification.
```

---

## What This Result Does Not Claim

This result does not claim:

```text
universal AI collapse prediction
semantic truth detection
domain-independent validity
production-level certification
final decision authority
model cognition detection
absolute external verification
external_source_verified status
live runtime validation
```

The result is valid only inside the tested v6 construction.

---

## Conclusion

Level 3 v6 passes as a declared external-source raw trajectory validation step.

The validator produced coherent regime separation:

```text
STABLE
DRIFT
CRITICAL
COLLAPSE
```

The aggregate result was:

```text
DRIFT
WATCH
```

The strongest trajectory was:

```text
external_source_collapse_like_001 -> COLLAPSE -> STOP
```

The borderline trajectory crossed into CRITICAL by a narrow margin:

```text
external_source_borderline_critical_001 -> 0.503233 -> CRITICAL
```

The decisive boundary condition is:

```text
source_independence: external_source_declared
independence_method: prompt_perturbation_trace
```

Therefore, v6 is stronger than v5 as a declared source-boundary validation, but it is still not absolute external verification.

The next step is to test the raw trajectory warning mechanism against truly externally produced records or public benchmark-derived traces where source origin can be documented independently.