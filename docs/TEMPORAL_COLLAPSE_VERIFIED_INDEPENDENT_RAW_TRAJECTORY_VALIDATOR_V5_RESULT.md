# Temporal Collapse Verified Independent Raw Trajectory Validator — v5 Result

## Status

PASS.

The Level 3 v5 verified independent raw trajectory validator executed successfully.

The generator script executed successfully.

The validator script executed successfully.

No runtime error was produced.

```text
GENERATOR STDERR: empty
GENERATOR RETURN CODE: 0

VALIDATOR STDERR: empty
VALIDATOR RETURN CODE: 0
```

Input file:

```text
data/temporal_collapse_verified_independent_raw_trajectories_v5.jsonl
```

Output file:

```text
results/temporal_collapse_verified_independent_raw_trajectory_validator_v5.json
```

Generator script:

```text
examples/generate_independent_raw_trajectories_v5.py
```

Validator script:

```text
examples/temporal_collapse_verified_independent_raw_trajectory_validator_v5.py
```

---

## Purpose

This experiment moves Level 3 from external-style validation to a stronger independent-generator boundary.

Level 3 v4 applied the raw trajectory warning mechanism to external-style trajectories with source independence marked as `not_verified`.

Level 3 v5 uses a separate generator script to produce raw ordered trajectory records.

The objective is to move from:

```text
external-style validation
```

to:

```text
separate-generator validation
```

```text
measurement != inference != decision
```

---

## Tested Boundary

This v5 result is bounded.

The tested boundary is:

```text
raw ordered trajectory records generated by a separate generator script
```

Source:

```text
independent_generator_script_v5
```

Source independence:

```text
generated_by_independent_script
```

Independence method:

```text
separate_generator_script
```

This is stronger than v4.

But it does not prove absolute external independence.

Correct reading:

```text
v5 validates the warning mechanism on raw trajectory records
generated by a separate generator script.
```

Incorrect reading:

```text
v5 proves universal independent validation.
```

---

## Input Dataset

The dataset contains five generated raw trajectories:

```text
independent_stable_001
independent_drift_001
independent_borderline_critical_001
independent_critical_001
independent_collapse_like_001
```

Each event contains:

```text
trajectory_id
step
signature
cluster
delta
iri
boundary_distance
phase
source
source_independence
independence_method
```

The validator groups events by `trajectory_id` and sorts each trajectory by `step`.

---

## Source Summary

The v5 source summary was:

```json
[
  {
    "source": "independent_generator_script_v5",
    "source_independence": "generated_by_independent_script",
    "independence_method": "separate_generator_script",
    "trajectory_count": 5,
    "average_risk_score": 0.444451,
    "regime_counts": {
      "DRIFT": 2,
      "COLLAPSE": 1,
      "CRITICAL": 1,
      "STABLE": 1
    },
    "highest_risk_trajectory": "independent_collapse_like_001",
    "highest_risk_score": 0.808922
  }
]
```

Interpretation:

All trajectories came from one separate generator script.

The independence method was explicitly recorded.

This improves the validation boundary compared to v4, where independence was marked as `not_verified`.

---

## Risk Formula

The v5 risk score preserves the raw trajectory formula used by v3 and v4:

```text
risk_score =
    0.20 * transition_density
  + 0.20 * drift_progression
  + 0.25 * boundary_proximity
  + 0.25 * collapse_similarity
  + 0.10 * irreversibility_signal
```

Weights:

```json
{
  "transition_density": 0.2,
  "drift_progression": 0.2,
  "boundary_proximity": 0.25,
  "collapse_similarity": 0.25,
  "irreversibility_signal": 0.1
}
```

Thresholds:

```text
risk_score < 0.25         -> STABLE
0.25 <= risk_score < 0.50 -> DRIFT
0.50 <= risk_score < 0.75 -> CRITICAL
risk_score >= 0.75        -> COLLAPSE
```

Gate actions:

```text
STABLE   -> PASS
DRIFT    -> WATCH
CRITICAL -> ESCALATE
COLLAPSE -> STOP
```

All weights and thresholds are visible.

No hidden interpretation layer is used.

---

## Aggregate Result

The aggregate v5 result was:

```text
aggregate_risk_score:  0.444451
aggregate_risk_regime: DRIFT
aggregate_gate_action: WATCH
source_count:          1
```

Regime counts:

```text
STABLE   -> 1
DRIFT    -> 2
CRITICAL -> 1
COLLAPSE -> 1
```

Highest-risk trajectory:

```text
independent_collapse_like_001 -> 0.808922
```

Interpretation:

The aggregate result is DRIFT because the dataset contains a mixed set of trajectories.

The main result is the ordered separation of risk regimes across generated raw trajectories.

---

## Ordered Risk Progression

The validator produced the following progression:

```text
independent_stable_001               -> STABLE   -> 0.037367 -> PASS
independent_drift_001                -> DRIFT    -> 0.285528 -> WATCH
independent_borderline_critical_001  -> DRIFT    -> 0.495100 -> WATCH
independent_critical_001             -> CRITICAL -> 0.595339 -> ESCALATE
independent_collapse_like_001        -> COLLAPSE -> 0.808922 -> STOP
```

The borderline trajectory is important:

```text
independent_borderline_critical_001 -> 0.495100
```

It remains just below the CRITICAL threshold:

```text
CRITICAL threshold: 0.50
```

This is a useful result.

The validator did not force the borderline case into CRITICAL.

It preserved the boundary.

---

## Individual Results

### independent_stable_001

```text
source:              independent_generator_script_v5
source_independence: generated_by_independent_script
independence_method: separate_generator_script
risk_regime:         STABLE
risk_score:          0.037367
gate_action:         PASS
dominant_axis:       boundary_proximity
warning_flags:       []
```

Signals:

```json
{
  "transition_density": 0.0,
  "drift_progression": 0.024333,
  "boundary_proximity": 0.09,
  "collapse_similarity": 0.0324,
  "irreversibility_signal": 0.019
}
```

Evidence:

```json
{
  "signature_changes": 0.0,
  "cluster_changes": 0.0,
  "phase_changes": 0.0,
  "delta_early_mean": 0.04,
  "delta_late_mean": 0.05,
  "iri_early_mean": 0.011,
  "iri_late_mean": 0.018,
  "min_boundary_distance": 0.905,
  "max_boundary_proximity": 0.095,
  "collapse_phase_count": 0,
  "source": "independent_generator_script_v5",
  "source_independence": "generated_by_independent_script",
  "independence_method": "separate_generator_script"
}
```

Interpretation:

The stable trajectory remained structurally consistent.

No warning flags were emitted.

---

### independent_drift_001

```text
source:              independent_generator_script_v5
source_independence: generated_by_independent_script
independence_method: separate_generator_script
risk_regime:         DRIFT
risk_score:          0.285528
gate_action:         WATCH
dominant_axis:       boundary_proximity
warning_flags:       []
```

Signals:

```json
{
  "transition_density": 0.333333,
  "drift_progression": 0.235556,
  "boundary_proximity": 0.44,
  "collapse_similarity": 0.193,
  "irreversibility_signal": 0.135
}
```

Evidence:

```json
{
  "signature_changes": 0.5,
  "cluster_changes": 0.25,
  "phase_changes": 0.25,
  "delta_early_mean": 0.105,
  "delta_late_mean": 0.296667,
  "iri_early_mean": 0.035,
  "iri_late_mean": 0.115,
  "min_boundary_distance": 0.52,
  "max_boundary_proximity": 0.48,
  "collapse_phase_count": 0,
  "source": "independent_generator_script_v5",
  "source_independence": "generated_by_independent_script",
  "independence_method": "separate_generator_script"
}
```

Interpretation:

The drift trajectory crossed out of STABLE and entered DRIFT.

No hard warning flags were emitted.

---

### independent_borderline_critical_001

```text
source:              independent_generator_script_v5
source_independence: generated_by_independent_script
independence_method: separate_generator_script
risk_regime:         DRIFT
risk_score:          0.495100
gate_action:         WATCH
dominant_axis:       boundary_proximity
warning_flags:
  - high_transition_density
  - boundary_proximity
```

Signals:

```json
{
  "transition_density": 0.583333,
  "drift_progression": 0.398833,
  "boundary_proximity": 0.7175,
  "collapse_similarity": 0.3415,
  "irreversibility_signal": 0.339167
}
```

Evidence:

```json
{
  "signature_changes": 0.75,
  "cluster_changes": 0.5,
  "phase_changes": 0.5,
  "delta_early_mean": 0.1825,
  "delta_late_mean": 0.51,
  "iri_early_mean": 0.085,
  "iri_late_mean": 0.298333,
  "min_boundary_distance": 0.235,
  "max_boundary_proximity": 0.765,
  "collapse_phase_count": 0,
  "source": "independent_generator_script_v5",
  "source_independence": "generated_by_independent_script",
  "independence_method": "separate_generator_script"
}
```

Interpretation:

The borderline trajectory produced a near-critical warning but remained DRIFT.

This is structurally useful because the validator preserved the threshold instead of over-classifying.

It sits immediately below the CRITICAL boundary.

---

### independent_critical_001

```text
source:              independent_generator_script_v5
source_independence: generated_by_independent_script
independence_method: separate_generator_script
risk_regime:         CRITICAL
risk_score:          0.595339
gate_action:         ESCALATE
dominant_axis:       boundary_proximity
warning_flags:
  - high_transition_density
  - boundary_proximity
```

Signals:

```json
{
  "transition_density": 0.75,
  "drift_progression": 0.478778,
  "boundary_proximity": 0.8075,
  "collapse_similarity": 0.4095,
  "irreversibility_signal": 0.453333
}
```

Evidence:

```json
{
  "signature_changes": 1.0,
  "cluster_changes": 0.75,
  "phase_changes": 0.5,
  "delta_early_mean": 0.25,
  "delta_late_mean": 0.638333,
  "iri_early_mean": 0.1225,
  "iri_late_mean": 0.401667,
  "min_boundary_distance": 0.15,
  "max_boundary_proximity": 0.85,
  "collapse_phase_count": 0,
  "source": "independent_generator_script_v5",
  "source_independence": "generated_by_independent_script",
  "independence_method": "separate_generator_script"
}
```

Interpretation:

The critical trajectory entered CRITICAL without requiring explicit COLLAPSE markers.

The validator emitted ESCALATE based on high transition density and boundary proximity.

---

### independent_collapse_like_001

```text
source:              independent_generator_script_v5
source_independence: generated_by_independent_script
independence_method: separate_generator_script
risk_regime:         COLLAPSE
risk_score:          0.808922
gate_action:         STOP
dominant_axis:       boundary_proximity
warning_flags:
  - high_transition_density
  - high_drift_progression
  - boundary_proximity
  - collapse_similarity
  - irreversibility_signal
```

Signals:

```json
{
  "transition_density": 0.75,
  "drift_progression": 0.644611,
  "boundary_proximity": 0.9375,
  "collapse_similarity": 0.8475,
  "irreversibility_signal": 0.8375
}
```

Evidence:

```json
{
  "signature_changes": 1.0,
  "cluster_changes": 0.75,
  "phase_changes": 0.5,
  "delta_early_mean": 0.3325,
  "delta_late_mean": 0.858333,
  "iri_early_mean": 0.2,
  "iri_late_mean": 0.745,
  "min_boundary_distance": 0.025,
  "max_boundary_proximity": 0.975,
  "collapse_phase_count": 2,
  "source": "independent_generator_script_v5",
  "source_independence": "generated_by_independent_script",
  "independence_method": "separate_generator_script"
}
```

Interpretation:

The collapse-like trajectory entered COLLAPSE.

All warning flags were activated.

The highest-risk trajectory was correctly identified as:

```text
independent_collapse_like_001
```

---

## Structural Reading

The v5 result extends v4 by improving the source boundary.

v4 used external-style raw trajectories with source independence marked as:

```text
not_verified
```

v5 uses raw trajectories generated by a separate generator script:

```text
generated_by_independent_script
```

with:

```text
independence_method: separate_generator_script
```

This is a stronger validation boundary.

But it is not absolute external independence.

The correct structural reading is:

```text
mechanism remains coherent over separately generated raw ordered trajectory records
```

not:

```text
mechanism is universally validated
```

---

## Main Finding

Safe claim:

```text
OMNIA-VALIDATION Level 3 v5 applied the raw trajectory warning mechanism
to raw ordered trajectory records generated by a separate generator script.

The validator separated STABLE, DRIFT, CRITICAL, and COLLAPSE regimes,
with one borderline near-critical trajectory preserved below the CRITICAL threshold.
```

---

## Important Limitation

This v5 result is still bounded.

The trajectories were generated by a separate script, but the setup remains internal to the repository.

Correct interpretation:

```text
Level 3 v5 validates the raw trajectory warning mechanism
on separately generated raw ordered trajectory records.
```

Not:

```text
Level 3 v5 proves absolute independent validation.
```

---

## What This Result Does Not Claim

This result does not claim:

```text
universal AI collapse prediction
semantic truth detection
domain-independent validity
production-level certification
final decision authority
model cognition detection
absolute external independence
live runtime validation
```

The result is valid only inside the tested v5 construction.

---

## Conclusion

Level 3 v5 passes as a separate-generator raw trajectory validation step.

The validator produced coherent regime separation:

```text
STABLE
DRIFT
CRITICAL
COLLAPSE
```

The aggregate result was:

```text
DRIFT
WATCH
```

The strongest trajectory was:

```text
independent_collapse_like_001 -> COLLAPSE -> STOP
```

The borderline trajectory was preserved as near-critical DRIFT:

```text
independent_borderline_critical_001 -> 0.495100 -> DRIFT
```

The decisive boundary condition is:

```text
source_independence: generated_by_independent_script
independence_method: separate_generator_script
```

Therefore, v5 is stronger than v4 but still not absolute external validation.

The next step is to test the raw trajectory warning mechanism against records produced outside this repository or by a clearly external model, benchmark, or trace source.